To run the following script
Go to the directory of the file and then type = export FLASK_APP=init.py
After this run =  python -m flask run
then open the browser and typw the localhost which shown on the cmd line.
